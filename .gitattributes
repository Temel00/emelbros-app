# Normalize line endings to LF in the repo so Windows checkouts and the Linux
# CI runner agree — keeps Prettier's format:check stable across platforms.
* text=auto eol=lf

# Binary assets git must not touch.
*.ico binary
*.png binary
*.jpg binary
*.woff2 binary
