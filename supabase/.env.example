# Copy to supabase/.env.local — the Supabase CLI loads it automatically and
# resolves config.toml's env(...) substitutions from it. Never commit the
# real supabase/.env.local.

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
