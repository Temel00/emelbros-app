## Agent skills

### Issue tracker

Issues are tracked as GitHub Issues in `Temel00/emelbros-app`, using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Agent buildout loop

How agent sessions work implementation tickets: model choice (efficient-first), agent-scoped tickets, draft-PR flow. See `docs/agents/agent-loop.md`.
